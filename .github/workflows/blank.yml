name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    name: echo Hello, Its PAUL testing the repository
        echo To all on the collaborative team MUKJUM 
        echo test, and deploy your project.
